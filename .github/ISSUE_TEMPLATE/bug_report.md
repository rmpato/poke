---
name: Bug report
about: Something behaves differently than it should
labels: bug
---

**What happened**

**What you expected**

**The command**

```bash
pogo curl ...
```

**Versions**

```
pogo --version
curl --version | head -1
```

<!-- Careful: request history and curl commands can contain tokens.
     In pogo, `y` then `C` copies a command with secrets masked. -->
